spring:
  application:
    name: carcare
  datasource:
    url: jdbc:postgresql://localhost:5432/mydatabase
    username: myuser
    password: secret
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: Europe/Skopje
  jackson:
    time-zone: Europe/Skopje
  mail:
    host: localhost
    port: 1025
    properties:
      mail:
        smtp:
          auth: false
          connectiontimeout: 2000
          timeout: 2000
          writetimeout: 2000
          starttls:
            enable: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  thymeleaf:
    check-template-location: false
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
app:
  time-zone: Europe/Skopje
  mail:
    from: no-reply@carcare.local
  jwt:
    secret: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
    access-minutes: 15
    refresh-days: 30
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
logging:
  level:
    com.delenicode.carcare: INFO
    org.hibernate.SQL: WARN
    org.hibernate.orm.jdbc.bind: WARN
